{
  "name": "lunar",
  "version": "1.0.0",
  "description": "lunar-lander-like game",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --noEmit && esbuild src/game.ts --bundle --sourcemap --minify --outfile=build/main.js && cp src/main.html build && serve build/",
    "production": "tsc --noEmit && esbuild src/game.ts --bundle --minify --outfile=build/main.js && cp src/main.html build && serve build/"
  },
  "author": "Max Viehl",
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.14.25",
    "serve": "^13.0.2"
  }
}
