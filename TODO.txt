pls rewrite the UI using litjs:

I want the #menu to be a component that gets a single state variable

let state: "init" | "ingame" | "won" | "lost" = "init"

the element <h2 id="game_mesg">(Will be replaced by JS.)</h2> should be empty on "init" and contain "You won!"/"You lost!" on the respective states.

The state "ingame" should hide the whole UI.





- changing config should reset state to "init" (hiding both messages)
- die Höhle weiterspielen nach Crash und Neustart irgendwie mit Tasten machen und NICHT so hässlich in die Game-Keyboard-IO einbauen, da stört das nur! Und nicht mit Control-Tasten gleich weiterspielen, sonst sieht man den crash-screen nicht! ("press any key" ist dann falsch!)



wenn nicht ingame, soll N/space auch das Menü resetten!
