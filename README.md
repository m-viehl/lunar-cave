# Lunar Cave game
Cool auto-generated caves!

# Build
- Clone this repo, run `npm install` and install the typescript compiler (e.g. `sudo apt install typescript`).
- Run `npm run build` or `npm run production`.
- Visit the displayed URL.

# Serve
Just serve the contents of the `build` directory.
