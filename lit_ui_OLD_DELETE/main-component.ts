/** 
 * one component that holds the whole UI, i.e. 
 * - now hold the GameState (to hide/show the whole ui) (and maybe pass it to childs if required)
 * - a tab widget to switch between challenge (highscores!) and custom game
 * - handle hiding/showing mouse (based on gamestate)
 * - should somehow distribute [ich schlaf ein...]
 * 
 * TODO nächster commit erst mal die Datei-Umstrukturierung,
 * dann das hier fertig denken und implementieren
 * 
 * state soll diesem Objekt gehören
 */